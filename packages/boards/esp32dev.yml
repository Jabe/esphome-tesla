# Default generic ESP32 board (esp32dev / 4MB).
substitutions:
  board: "esp32dev"
  variant: "ESP32"
  flash_size: "4MB"

esp32:
  board: $board
  variant: $variant
  flash_size: $flash_size
  framework:
    type: esp-idf
